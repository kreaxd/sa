module.exports.operate = async ({client, msg, args,auth, author}) => {
    if (!author.permissions.has("ADMINISTRATOR") return;
    let rolsuz = msg.guild.members.cache.filter.

};
    
  module.exports.help = {
    name: "jail",
    alias: ["cezalı", "cezali"]
  };


 